    # ITI: el vendedor paga IG? Si es si se agota todo,no paga ITI, si es no, se siguecon estas preguntas : 
    # hace uso del reemplazo ? Si/no . Si es si no paga y si es no, paga. Si paga hay que pasar el precio a
    # moneda local si es que está en dólares. Al resultado aplicarle el 1,5%

    # IG paga ganancias ? Si es si hay que pasar el precio a moneda local si es que está en dólares y al
    # resultado compararlo con la VF. Al que sea mayor hay que aplicarle el 3%

    # Sellos. Comprador tiene otra propiedad? Si /no. Si tiene paga hay que pasar el precio a moneda local
    # si es que está en dólares y al resultado hay que compararlo con la VF y el VIR. Al que sea mayor hay
    # que aplicarle el 3,6%. Si no tiene otra propiedad, hay que pasar el precio a pesos y ver si supera 975000$.
    # Si no lo supera, no paga. Si lo supera hay que comparar Precio VF y VIR y al que sea mayor hay que restarle 975000.
    # A la diferencia hay que aplicarle el 3,6%